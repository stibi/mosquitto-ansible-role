---
# defaults file for mosquitto-arch-ansible-role
paths:
  cafile: "/etc/mosquitto/ca/ca.crt"
  certfile: "/etc/mosquitto/certs/{{ ansible_hostname }}.crt"
  keyfile: "/etc/mosquitto/certs/{{ ansible_hostname }}.key"